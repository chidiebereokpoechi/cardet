app:
  secret: whot.io
  port: 8080

database:
  type: mysql
  host: localhost
  port: 3306
  database: whot.io
  username: whot.io
  password: whot.io
  name: default
  entities:
    - dist/database/*.entity.js
  synchronize: true
  cache: false
  supportsJson: true
  # dropSchema: true
